APP_NAME=logistics
APP_ENV=local
APP_KEY=base64:DI+l9LoFc+kQzXCxGlNarUUo3DaGfNY0E8kMPkC+yMU=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlsrv
DB_HOST=DESKTOP
DB_PORT=1433
DB_DATABASE=logistics
DB_USERNAME=sa
DB_PASSWORD=123123
DB_ENCRYPT=yes
DB_TRUST_SERVER_CERTIFICATE=true


SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=pusher
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

PUSHER_APP_ID=logistics-app
PUSHER_APP_KEY=logistics-app-key
PUSHER_APP_SECRET=logistics-app-secret
PUSHER_HOST=127.0.0.1
PUSHER_PORT=6001
PUSHER_SCHEME=http
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Laravel Telescope (Development only)
TELESCOPE_ENABLED=false
TELESCOPE_PRUNE_HOURS=48

# Spatie Ray (Development debugging)
RAY_ENABLED=true
RAY_HOST=127.0.0.1
RAY_PORT=23517

# Laravel Debugbar (Development only)
DEBUGBAR_ENABLED=false

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1    

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Geocoding (adres -> enlem/boylam). Varsayılan: Nominatim (ücretsiz)
# GEOCODING_PROVIDER=nominatim
# GEOCODING_NOMINATIM_URL=https://nominatim.openstreetmap.org/search
# GEOCODING_NOMINATIM_EMAIL=
# GOOGLE_MAPS_GEOCODING_API_KEY=
# GEOCODING_USER_AGENT="${APP_NAME}/1.0"

# SAP Integration
SAP_SYNC_ENABLED=false
SAP_AUTO_SYNC=false
SAP_ODATA_URL=https://sap-system:port/sap/opu/odata/sap
SAP_USERNAME=
SAP_PASSWORD=
SAP_TIMEOUT=30
SAP_VERIFY_SSL=true
SAP_RETRY_ATTEMPTS=3
SAP_RETRY_DELAY=5
SAP_BATCH_SIZE=50

# LOGO ERP Integration
LOGO_INTEGRATION_ENABLED=false
LOGO_API_URL=https://logo-api.example.com/api
LOGO_API_TOKEN=
LOGO_TIMEOUT=30
LOGO_AUTO_SYNC_CUSTOMERS=false
LOGO_AUTO_SYNC_INVOICES=false
LOGO_AUTO_SYNC_ORDERS=false

# E-Invoice (GIB) Integration
EINVOICE_ENABLED=false
EINVOICE_GIB_URL=https://efaturatest.gbonline.com.tr/services
EINVOICE_GIB_USERNAME=
EINVOICE_GIB_PASSWORD=
EINVOICE_TIMEOUT=30
EINVOICE_CERT_PATH=storage/certificates
EINVOICE_CERT_PASSWORD=
EINVOICE_AUTO_GENERATE=false
EINVOICE_AUTO_SEND=false
EINVOICE_E_ARCHIVE_ENABLED=false
EINVOICE_E_ARCHIVE_THRESHOLD=5000
